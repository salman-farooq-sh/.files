cd ~/Programming/esketchers/mentalhealthmatch/apps/web/
launch fish --interactive --init-command 'nvm use 18.15.0 && NODE_OPTIONS=--openssl-legacy-provider npm run dev:ssr'

# new_tab
# cd ~/Programming/esketchers/mentalhealthmatch/apps/web/
# launch fish --interactive --init-command 'nvm use 18.15.0 && NODE_OPTIONS=--openssl-legacy-provider npm start'

new_tab
cd ~/Programming/esketchers/mentalhealthmatch/apps/web/
launch fish --interactive --init-command 'nvm use 18.15.0 && hx'
