cd ~/Programming/esketchers/mentalhealthmatch/apps/web/
launch low fish --interactive --init-command 'nvm use && NODE_OPTIONS=--openssl-legacy-provider npm run dev:ssr'

# new_tab
# cd ~/Programming/esketchers/mentalhealthmatch/apps/web/
# launch low fish --interactive --init-command 'nvm use && NODE_OPTIONS=--openssl-legacy-provider npm start'

new_tab
cd ~/Programming/esketchers/mentalhealthmatch/apps/web/
launch fish --interactive --init-command 'nvm use && hx'
